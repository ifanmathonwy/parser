This library currently implements the Earley parsing
algorithm.

Hopefully, in the future, it will also implement
other parsing algorithms, such as CKY. The addition
of probabilistic parsing is also on the roadmap.
